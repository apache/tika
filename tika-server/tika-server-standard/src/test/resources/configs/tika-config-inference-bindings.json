{
  "engines": { "mock": { "mock-engine": {} } },
  "inference": [
    { "id": "mock-images", "engine": "mock", "input": "IMAGES", "tasks": ["mock-task"] },
    { "id": "mock-pages", "engine": "mock", "input": "PAGES", "tasks": ["mock-task"] },
    { "id": "mock-text", "engine": "mock", "input": "TEXT", "tasks": ["mock-task"] }
  ],
  "auto-detect-parser": { "throwOnZeroBytes": false },
  "parse-context": { "timeout-limits": { "progressTimeoutMillis": 30000 } }
}
